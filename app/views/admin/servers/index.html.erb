<div class="bs-docs-example">
 <form class="form-search i_form_search">
   <input type="text" class="input-medium search-query i_detail_backend_s">
   <button type="submit" class="btn">Search</button>
 </form>
 <div class="new_new">
   <%= link_to "Thêm mới", new_admin_server_path, :class => "btn btn-success"%>
 </div>
 <table class="table">
  <thead>
   <tr>
    <th>#</th>
    <th>Tên server</th>
    <th>Action</th>
   </tr>
  </thead>
  <tbody>
   <% @admin_servers.each do |as|%>
   <tr class="<%= as.id % 2 == 0 ? 'success': ''%>">
    <td><%= as.id%></td>
    <td><%= as.name%></td>
    <td>
     <%= link_to image_tag("#{asset_path('action_view.png')}"), admin_server_path(as), :title => "Chi tiết server"%>
     <%= link_to image_tag("#{asset_path('action_edit.png')}"), edit_admin_server_path(as), :title => "Sửa"%>
     <%= link_to image_tag("#{asset_path('action_delete.png')}"), admin_server_path(as), :confirm => "Bạn muốn xóa server này?!", :method => :delete, :title => "Xóa"%>
    </td>
   </tr>
   <% end %>
  </tbody>
 </table>
</div>